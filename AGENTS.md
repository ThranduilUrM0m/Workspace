# AGENTS.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues (ThranduilUrM0m/Workspace), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context layout — one `CONTEXT.md` per project (Boutaleb, Maxconfort, Qasidaty, Risala) plus the shared `packages/ui`, with a root `CONTEXT-MAP.md` pointing to each. See `docs/agents/domain.md`.
